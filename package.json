{
  "name": "ksot-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "sync": "npm run sync --prefix web",
    "upload:journal": "node scripts/upload-journal-pdfs.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "dotenv": "^16.4.7"
  }
}
